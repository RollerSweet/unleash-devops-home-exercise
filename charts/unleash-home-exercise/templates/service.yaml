apiVersion: v1
kind: Service
metadata:
  name: {{ include "unleash-home-exercise.fullname" . }}
  labels:
    {{- include "unleash-home-exercise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "unleash-home-exercise.selectorLabels" . | nindent 4 }}
